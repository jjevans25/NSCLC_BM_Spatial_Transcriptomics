# ---------------------------------------------------------------------------
# Phase 6 — FAIR packaging: RO-Crate, report, clean-room reproduction.
#
# Owner task: Phase 6 (see Markdowns/PROJECT_PLAN.md §6).
# STUB — no rules yet. TARGETS_FAIR stays empty so `snakemake -n` reports an
# empty DAG rather than a missing-input error. Populate both the rules and
# this list in the same commit.
# ---------------------------------------------------------------------------

TARGETS_FAIR = []
