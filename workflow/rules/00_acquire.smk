# ---------------------------------------------------------------------------
# Phase 0 — acquisition: GEO download, checksums, idempotent re-run.
#
# Owner task: P0-T2 (see Markdowns/PROJECT_PLAN.md §6).
# STUB — no rules yet. TARGETS_ACQUIRE stays empty so `snakemake -n` reports an
# empty DAG rather than a missing-input error. Populate both the rules and
# this list in the same commit.
# ---------------------------------------------------------------------------

TARGETS_ACQUIRE = []
