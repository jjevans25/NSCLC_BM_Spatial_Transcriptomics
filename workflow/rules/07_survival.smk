# ---------------------------------------------------------------------------
# Phase 5 — stretch: prognostic association. Gated entirely on Q5.
#
# Owner task: Phase 5 (see Markdowns/PROJECT_PLAN.md §6).
# STUB — no rules yet. TARGETS_SURVIVAL stays empty so `snakemake -n` reports an
# empty DAG rather than a missing-input error. Populate both the rules and
# this list in the same commit.
# ---------------------------------------------------------------------------

TARGETS_SURVIVAL = []
