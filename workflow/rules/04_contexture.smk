# ---------------------------------------------------------------------------
# Phase 2 — immune contexture, brain vs. lung.
#
# Owner task: Phase 2 (see Markdowns/PROJECT_PLAN.md §6).
# STUB — no rules yet. TARGETS_CONTEXTURE stays empty so `snakemake -n` reports an
# empty DAG rather than a missing-input error. Populate both the rules and
# this list in the same commit.
# ---------------------------------------------------------------------------

TARGETS_CONTEXTURE = []
