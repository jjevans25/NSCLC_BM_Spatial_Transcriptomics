# ---------------------------------------------------------------------------
# Phase 4 — inferred cross-compartment crosstalk (never 'colocalisation').
#
# Owner task: Phase 4 (see Markdowns/PROJECT_PLAN.md §6).
# STUB — no rules yet. TARGETS_CROSSTALK stays empty so `snakemake -n` reports an
# empty DAG rather than a missing-input error. Populate both the rules and
# this list in the same commit.
# ---------------------------------------------------------------------------

TARGETS_CROSSTALK = []
