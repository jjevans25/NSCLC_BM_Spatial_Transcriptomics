# ---------------------------------------------------------------------------
# Phase 0 — annotation: sample-title parsing, design matrix, .h5ad export.
#
# Owner task: P0-T3, P0-T7 (see Markdowns/PROJECT_PLAN.md §6).
# STUB — no rules yet. TARGETS_ANNOTATE stays empty so `snakemake -n` reports an
# empty DAG rather than a missing-input error. Populate both the rules and
# this list in the same commit.
# ---------------------------------------------------------------------------

TARGETS_ANNOTATE = []
