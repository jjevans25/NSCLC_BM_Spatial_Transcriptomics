# ---------------------------------------------------------------------------
# Phase 0 — QC and normalisation (R), marker sanity check, power check.
#
# Owner task: P0-T5, P0-T6, P0-T8 (see Markdowns/PROJECT_PLAN.md §6).
# STUB — no rules yet. TARGETS_QC stays empty so `snakemake -n` reports an
# empty DAG rather than a missing-input error. Populate both the rules and
# this list in the same commit.
# ---------------------------------------------------------------------------

TARGETS_QC = []
