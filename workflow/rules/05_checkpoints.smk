# ---------------------------------------------------------------------------
# Phase 3 — checkpoint landscape by compartment.
#
# Owner task: Phase 3 (see Markdowns/PROJECT_PLAN.md §6).
# STUB — no rules yet. TARGETS_CHECKPOINTS stays empty so `snakemake -n` reports an
# empty DAG rather than a missing-input error. Populate both the rules and
# this list in the same commit.
# ---------------------------------------------------------------------------

TARGETS_CHECKPOINTS = []
